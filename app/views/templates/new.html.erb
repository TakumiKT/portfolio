<div class="container">
  <section class="card">
    <h1 class="page-title">テンプレート作成</h1>

    <%= form_with model: @template, local: true do |f| %>
      <%= render "form", f: f, template: @template %>
    <% end %>
  </section>
</div>